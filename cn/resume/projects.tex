%-------------------------------------------------------------------------------
%\tSECTION TITLE
%-------------------------------------------------------------------------------
\cvsection{Projects}
%-------------------------------------------------------------------------------
%\tCONTENT
%-------------------------------------------------------------------------------
\begin{cventries}
  \cventry
  {}
  {AIO Asset Normalizer}
  {Rust, egui, Codex, Blender API}
  {Jul. 2026 - Dec. 2026}
  {
    \begin{cvitems}
      \item{设计并使用 \textbf{Rust} 与 \textbf{egui} 实现 native asset normalization application，支持通过 GUI 和 CLI interfaces 将 FBX、BVH 转换为 GLB，并支持 asset trimming、editing、normalization、tuning 以及 batch processing。}
      \item{将该 application 集成到 indie game development workflow 中，简化 asset processing，减少 asset redundancy 和 inconsistency，加速 game iteration。}
      \item{开发 internal AI agent 进行 bone mapping：一键为一对 GLB/BVH files 生成 bone-mapping configurations，将 AI-assisted setup 集成到 art-asset standardization pipeline。}
    \end{cvitems}
  }

  \clearpage
  %---------------------------------------------------------
  \cventry
  {}
  {Augur Git (Open Source Project)}
  {Rust, gpui, Codex}
  {Jul. 2026 - Sep. 2026}
  {
    \begin{cvitems}
      \item{使用 Rust 和 gpui 构建 cross-platform、local-first desktop Git client，支持 multi-repository tabs、persistent settings、themes、localization，以及 Windows、macOS 和 Linux 上的 responsive UI。}
      \item{通过 background workers、defensive parsers、syntax-aware inline and side-by-side diffs、commit graphs、branch comparison，以及明确的 staging、commit、fetch、pull、push operations，构建 review-first Git workflow。}
      \item{集成可配置的 Codex、Claude Code 和 OpenCode profiles、visible agent sessions、AI-assisted commit/merge/rebase workflows，以及 Lua 5.4 automation；在整个 development 过程中广泛使用 AI，并以 Codex 作为 primary coding agent。}
    \end{cvitems}
  }

  %---------------------------------------------------------
  \cventry
  {}
  {License Plate Recognition Systems -- Course Project}
  {Python, PyTorch}
  {Jan. 2026 -- Apr. 2026}
  {
    \begin{cvitems}
      \item{设计 deep learning based License Plate Recognition System，构建 CNN+CRNN neural network，编写用于 training 和 inference 的 PyTorch code，并完成 model training 和 testing。}
      \item{构建 Python pipeline，批量生成 license plate images、添加 noise 并将其与 background tile，自动生成 10,000 个包含完整 vehicle background 的 training data points。}
      \item{使用 Python 和 \texttt{uv} 作为 package manager，建立完整的 data generation 和 training pipeline，并编写完整 project documentation，确保 training results 高度 reproducible。}
    \end{cvitems}
  }

  %---------------------------------------------------------
  \cventry
  {}
  {Dinosaur Game on STM32 MCU -- Course Project}
  {C, STM32, Embedded Systems}
  {Mar. 2026}
  {
    \begin{cvitems}
      \item{使用 \textbf{C} 在 \textbf{STM32F446ZE Nucleo Board} 上开发 2D Dinosaur Game，实现 gameplay、GPIO 和 I2C peripherals 的 driver、double buffering video engine，并在 3 周内交付 playable game。}
      \item{集成 4*3 keypad 进行 real-time user control，利用 \textbf{CPU interrupts} 实现 low-latency input handling。}
      \item{构建 lightweight、OS-independent 2D graphics engine，以 double buffering 和 DMA 驱动 2 个 SSD1306 OLED displays。}
    \end{cvitems}
  }

  %---------------------------------------------------------
  \cventry
  {}
  {GIS Route Optimization Application -- Course Project}
  {C++, GTK, Git, A*, Dijkstra}
  {Jan. 2025 -- Apr. 2025}
  {
    \begin{cvitems}
      \item{在 3-person team 中，使用 \textbf{C++} 和 \textbf{GTK} 在 Mate Desktop 上开发 GIS desktop application，实现 map rendering、geographical name search、shortest path 和 multi-stop path finding features。}
      \item{利用 \textbf{multithreading} 预处理 4GB raw coordinate data，将其转换为 structured point、line 和 polygon formats，以便在 16-core test machine 上完成 canvas rendering，耗时低于 50 秒。}
      \item{实现 pathfinding algorithms，包括用于 shortest path computation 的 \textbf{A*}，以及用于 multi-stop route optimization 的 \textbf{Dijkstra}、\textbf{multi-start greedy} 和 \textbf{simulated annealing}，获得 technical evaluation score 的 90\%。}
      \item{使用 \textbf{TomTom API} data 和 \textbf{libcurl} HTTP module 集成 real-time traffic visualization。}
    \end{cvitems}
  }

  %---------------------------------------------------------
  \cventry
  {}
  {Runner Game (FPGA Board Game) -- Course Project}
  {C, RISC-V Assembly, FPGA Board, CPUlator}
  {Mar. 2025}
  {
    \begin{cvitems}
      \item{使用 \textbf{C} 在 \textbf{DE1-SoC FPGA board} 上开发 2D runner game，实现 core game logic、PS/2 keyboard、audio components 和 video components，并在 3 周内交付 playable game。}
      \item{集成 PS/2 keyboard 进行 real-time user control，利用 \textbf{CPU interrupts} 实现 low-latency input handling。}
      \item{构建 lightweight、OS-independent 2D graphics engine，以最高 60 FPS 渲染 color images、animations 和 text。}
      \item{开发 basic square wave synthesizer，在 scoring 和 game completion 等 gameplay events 中实现 dynamic sound effects。}
    \end{cvitems}
  }

  %---------------------------------------------------------
  \cventry
  {}
  {TradeFlow System (Open Source)}
  {TypeScript, Node.js, PostgreSQL, React}
  {Jul. 2025 -- Present}
  {
    \begin{cvitems}
      \item{设计 full-stack trade management solution，包含 inventory 和 financial tracking 以及 sales analysis。}
      \item{使用 \textbf{TypeScript}、\textbf{Node.js} 和 \textbf{PostgreSQL} 构建 high-performance backend。}
      \item{使用 \textbf{React} 和 \textbf{Ant Design} 开发 responsive frontend，采用 component-driven architecture 加速 feature delivery 并保持 code reusability。}
    \end{cvitems}
  }
\end{cventries}
